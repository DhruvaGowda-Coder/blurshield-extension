rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /trials/{userId} {
      // Only the authenticated user can read or write their own trial document.
      allow read, write: if request.auth != null && request.auth.uid == userId;
    }
  }
}
